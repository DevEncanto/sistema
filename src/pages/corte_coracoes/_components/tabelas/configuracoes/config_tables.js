import { lista_etiquetas } from "./lista_etiquetas";
import { lotes_etiquetas } from "./lotes_etiquetas";
import { medias_cachos } from "./medias_cachos";
import { previsoes_mensais } from "./previsoes_mensais";

export const config_tables = {
    lotes_etiquetas,
    lista_etiquetas,
    previsoes_mensais,
    medias_cachos
};
